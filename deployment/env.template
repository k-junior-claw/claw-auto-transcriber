# Claw Auto-Transcriber Environment Configuration
# Copy to /etc/claw-transcriber/env and set your values
#
# This file is used by the systemd service.
# Make sure to restrict permissions: sudo chmod 600 /etc/claw-transcriber/env

# =============================================================================
# REQUIRED: Google Cloud Configuration
# =============================================================================

# Your Google Cloud Project ID
GOOGLE_CLOUD_PROJECT_ID=your-project-id

# Path to Google Cloud service account credentials JSON file
GOOGLE_APPLICATION_CREDENTIALS=/etc/claw-transcriber/credentials/service-account.json

# =============================================================================
# OPTIONAL: MCP Server Configuration
# =============================================================================

# Server name for identification (default: claw-auto-transcriber)
MCP_SERVER_NAME=claw-auto-transcriber

# =============================================================================
# OPTIONAL: Audio Processing
# =============================================================================

# Maximum audio duration in seconds (default: 60)
MAX_AUDIO_DURATION=60

# Maximum audio file size in bytes (default: 10MB)
MAX_AUDIO_SIZE=10485760

# Default language for transcription (BCP-47 format, default: en-US)
DEFAULT_LANGUAGE_CODE=en-US

# Temp directory for audio processing (default: /tmp/claw_transcriber)
TEMP_AUDIO_DIR=/tmp/claw_transcriber

# =============================================================================
# OPTIONAL: Logging
# =============================================================================

# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL (default: INFO)
LOG_LEVEL=INFO

# Log format: json or text (default: json)
LOG_FORMAT=json

# =============================================================================
# OPTIONAL: Performance
# =============================================================================

# Timeout for transcription in seconds (default: 30)
TRANSCRIPTION_TIMEOUT=30

# Maximum retry attempts for failed transcriptions (default: 3)
MAX_RETRY_ATTEMPTS=3

# Retry delay in seconds (default: 1)
RETRY_DELAY=1
